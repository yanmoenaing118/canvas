/**
 *  A thousand milliseconds in a second, divided by 60, gives us 60 frames per second.  setInterval will run unceasingly and forever.
 * The Loop will re-run 60 times in 1s, which also means a loop will take about 1000ms/60 = 16.6666666...ms
 * 
 * 
 * THE LOOP runs 60 times in 1s
 * 
 * requestAnimationFrame(Loop)
 */
